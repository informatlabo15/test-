import { FilterAsignaturaPipe } from './filter-asignatura.pipe';

describe('FilterAsignaturaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAsignaturaPipe();
    expect(pipe).toBeTruthy();
  });
});
